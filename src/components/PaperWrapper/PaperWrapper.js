import {styled} from '@mui/system';
import {Paper} from "@mui/material"


export const PaperWrapper = styled(Paper)({
    paddingTop: 10,
    paddingBottom: 10,
    paddingLeft: 15,
    paddingRight: 15,
    marginBottom: '2rem',
    marginTop: '1rem'
})

